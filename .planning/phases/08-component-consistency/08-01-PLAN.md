---
phase: 08-component-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/medicare-101.njk
  - src/pages/medigap-vs-mapd.njk
  - src/pages/enrollment-periods.njk
  - src/pages/part-d.njk
  - src/_includes/partials/nav.njk
  - src/index.njk
autonomous: true
requirements:
  - PAGE-01
  - PAGE-05

must_haves:
  truths:
    - "All 4 guide pages (medicare-101, medigap-vs-mapd, enrollment-periods, part-d) use the same section structure: .article-section containing .section-label + .section-rule + h2"
    - "No template or built _site/ file contains the class combination 'btn btn-primary' (double-class)"
    - "Every button in the site is exclusively .btn-primary, .btn-outline, or .btn-outline-light"
  artifacts:
    - path: "src/pages/medicare-101.njk"
      provides: "Guide page with canonical section structure"
      contains: "article-section"
    - path: "src/pages/medigap-vs-mapd.njk"
      provides: "Guide page with canonical section structure"
      contains: "article-section"
    - path: "src/pages/enrollment-periods.njk"
      provides: "Guide page with canonical section structure"
      contains: "article-section"
    - path: "src/pages/part-d.njk"
      provides: "Guide page with canonical section structure"
      contains: "article-section"
    - path: "src/_includes/partials/nav.njk"
      provides: "Nav with single .btn-primary class (not double)"
  key_links:
    - from: "src/pages/*.njk (guide pages)"
      to: ".article-section > .section-label + .section-rule + h2"
      via: "markup pattern"
      pattern: "section-label.*section-rule"
    - from: "src/_includes/partials/nav.njk"
      to: ".btn-primary (single class)"
      via: "anchor class attribute"
      pattern: "btn-primary"
---

<objective>
Audit and confirm the canonical section structure (PAGE-01) is present on all 4 guide pages, and clean up the button double-class violation (PAGE-05) across all templates.

Purpose: Establishes the authoritative section pattern as a verified baseline before the callout and partial work in sibling plans. Eliminates the `btn btn-primary` redundancy that conflicts with the locked three-variant button system.
Output: 4 verified guide pages with canonical section structure; no `btn btn-primary` double-class in any template.
</objective>

<execution_context>
@/home/timothywinslowlinux/.claude/get-shit-done/workflows/execute-plan.md
@/home/timothywinslowlinux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-component-consistency/08-CONTEXT.md
</context>

<interfaces>
<!-- Canonical section structure already established in guide pages. -->
<!-- Executor should verify this pattern is present; minimal markup changes expected. -->

Canonical section structure (from medicare-101.njk, confirmed in all 4 guides):
```html
<section class="article-section" id="{id}" aria-labelledby="{id}-heading">
  <span class="section-label">Section 0N</span>
  <span class="section-rule" aria-hidden="true"></span>
  <h2 id="{id}-heading">{Heading text}</h2>
  <!-- section content -->
</section>
```

Current nav.njk button (line 10) — needs double-class cleanup:
```html
<a href="#newsletter" class="btn btn-primary">Subscribe Free</a>
```
Fix: remove the redundant `btn` class, keep only `btn-primary`:
```html
<a href="#newsletter" class="btn-primary">Subscribe Free</a>
```

Other `btn btn-primary` occurrences to fix (same pattern):
- src/index.njk line ~34: `class="btn btn-primary"` → `class="btn-primary"`
- src/index.njk line ~117: `class="btn btn-primary btn-full"` → `class="btn-primary btn-full"`
- src/index.njk line ~426: `class="btn btn-primary"` → `class="btn-primary"`
- src/pages/about.njk line ~236: `class="btn btn-primary"` → `class="btn-primary"`
- src/pages/work-with-me.njk line ~287: `class="btn btn-primary"` → `class="btn-primary"`

Keep as-is (locked decision — contact-btn is a specialized phone/SMS tap target):
- src/pages/work-with-me.njk: `contact-btn`, `contact-btn-call`, `contact-btn-text`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify canonical section structure on all 4 guide pages</name>
  <files>src/pages/medicare-101.njk, src/pages/medigap-vs-mapd.njk, src/pages/enrollment-periods.njk, src/pages/part-d.njk</files>
  <action>
    Grep each of the 4 guide pages for `article-section`, `section-label`, and `section-rule` to confirm all three classes are present in every section block. The canonical pattern is:

    ```html
    <section class="article-section" id="{id}" aria-labelledby="{id}-heading">
      <span class="section-label">Section 0N</span>
      <span class="section-rule" aria-hidden="true"></span>
      <h2 id="{id}-heading">...</h2>
    ```

    If any section in any of the 4 guide pages is missing `.section-label` or `.section-rule`, add the missing element in the correct position (immediately before the h2, in that order: section-label then section-rule). Do NOT change heading text or section IDs.

    Expected finding based on codebase audit: all 4 pages already use the canonical pattern — this is a verification pass, not a rewrite. Make no changes if already correct.
  </action>
  <verify>
    <automated>grep -c "section-label" /home/timothywinslowlinux/retiree-report/src/pages/medicare-101.njk /home/timothywinslowlinux/retiree-report/src/pages/medigap-vs-mapd.njk /home/timothywinslowlinux/retiree-report/src/pages/enrollment-periods.njk /home/timothywinslowlinux/retiree-report/src/pages/part-d.njk && grep -c "section-rule" /home/timothywinslowlinux/retiree-report/src/pages/medicare-101.njk /home/timothywinslowlinux/retiree-report/src/pages/medigap-vs-mapd.njk /home/timothywinslowlinux/retiree-report/src/pages/enrollment-periods.njk /home/timothywinslowlinux/retiree-report/src/pages/part-d.njk</automated>
  </verify>
  <done>Each of the 4 guide pages has at least 1 occurrence of both `section-label` and `section-rule`. Every `article-section` block in every guide page contains a `.section-label` span and a `.section-rule` span immediately before the h2.</done>
</task>

<task type="auto">
  <name>Task 2: Remove btn double-class violations (PAGE-05)</name>
  <files>src/_includes/partials/nav.njk, src/index.njk, src/pages/about.njk, src/pages/work-with-me.njk</files>
  <action>
    Find every occurrence of `btn btn-primary` (the redundant double-class) across all templates and replace with `btn-primary` (single class). Also fix any `btn btn-outline` or `btn btn-outline-light` double-class patterns if found.

    Specific replacements:
    1. src/_includes/partials/nav.njk: `class="btn btn-primary"` → `class="btn-primary"`
    2. src/index.njk: all `class="btn btn-primary"` → `class="btn-primary"` (3 occurrences); `class="btn btn-primary btn-full"` → `class="btn-primary btn-full"`
    3. src/pages/about.njk: `class="btn btn-primary"` → `class="btn-primary"`
    4. src/pages/work-with-me.njk: `class="btn btn-primary"` → `class="btn-primary"` (keep `contact-btn`, `contact-btn-call`, `contact-btn-text` unchanged — these are specialized phone/SMS tap targets, not generic CTAs, per locked decision)

    Do NOT touch `btn-outline-light` usages that appear without the redundant `btn` prefix — those are already correct.
  </action>
  <verify>
    <automated>grep -rn "class=\"btn btn-primary\"\|class=\"btn btn-outline\"" /home/timothywinslowlinux/retiree-report/src/ && echo "FOUND violations" || echo "CLEAN — no double-class violations"</automated>
  </verify>
  <done>Grep for `btn btn-primary` across all src/ files returns zero matches. Every button uses exactly one of: `.btn-primary`, `.btn-outline`, `.btn-outline-light`, `.btn-primary.btn-full`. The `contact-btn` family on work-with-me.njk is untouched.</done>
</task>

</tasks>

<verification>
Run the Eleventy build to confirm no template errors were introduced:
```
cd /home/timothywinslowlinux/retiree-report && npx @11ty/eleventy --quiet 2>&1 | tail -5
```
Build must complete with 0 errors. Confirm `_site/` was generated successfully.
</verification>

<success_criteria>
- All 4 guide pages confirmed to use canonical `.article-section` > `.section-label` + `.section-rule` + `h2` pattern
- Zero occurrences of `btn btn-primary` double-class in any template file under src/
- Eleventy build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-component-consistency/08-01-SUMMARY.md`
</output>
