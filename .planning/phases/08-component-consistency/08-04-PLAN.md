---
phase: 08-component-consistency
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_includes/partials/newsletter-cta.njk
  - src/index.njk
  - src/pages/about.njk
  - src/pages/work-with-me.njk
  - src/pages/medicare-101.njk
  - src/pages/medigap-vs-mapd.njk
  - src/pages/enrollment-periods.njk
  - src/pages/part-d.njk
autonomous: true
requirements:
  - PAGE-04

must_haves:
  truths:
    - "A file src/_includes/partials/newsletter-cta.njk exists and contains the newsletter form (heading, form, fine print)"
    - "Every page that previously had a newsletter signup form now uses {% include 'partials/newsletter-cta.njk' %} for the form portion"
    - "Editing newsletter-cta.njk changes the form on all 7 pages simultaneously"
    - "Article pages (medicare-101, medigap-vs-mapd, enrollment-periods, part-d) retain their next-steps grid — only the form at the bottom is replaced by the partial include"
  artifacts:
    - path: "src/_includes/partials/newsletter-cta.njk"
      provides: "Single authoritative newsletter form partial"
      exports: ["heading", "form", "fine-print"]
    - path: "src/index.njk"
      provides: "Uses newsletter-cta.njk partial directly"
      contains: "include 'partials/newsletter-cta.njk'"
    - path: "src/pages/about.njk"
      provides: "Uses newsletter-cta.njk partial directly"
      contains: "include 'partials/newsletter-cta.njk'"
    - path: "src/pages/work-with-me.njk"
      provides: "Uses newsletter-cta.njk partial directly"
      contains: "include 'partials/newsletter-cta.njk'"
    - path: "src/pages/medicare-101.njk"
      provides: "Form portion replaced by partial, next-steps grid retained"
      contains: "include 'partials/newsletter-cta.njk'"
    - path: "src/pages/medigap-vs-mapd.njk"
      provides: "Form portion replaced by partial, next-steps grid retained"
      contains: "include 'partials/newsletter-cta.njk'"
    - path: "src/pages/enrollment-periods.njk"
      provides: "Form portion replaced by partial, next-steps grid retained"
      contains: "include 'partials/newsletter-cta.njk'"
    - path: "src/pages/part-d.njk"
      provides: "Form portion replaced by partial, next-steps grid retained"
      contains: "include 'partials/newsletter-cta.njk'"
  key_links:
    - from: "src/_includes/partials/newsletter-cta.njk"
      to: "all 7 pages via {% include %}"
      via: "Nunjucks include statement"
      pattern: "include.*newsletter-cta"
---

<objective>
Create the `newsletter-cta.njk` partial containing the newsletter signup form (heading, input, button, fine print), then replace the inline newsletter form in all 7 pages with a single include statement. Article pages retain their next-steps grid; only the form portion moves to the partial.

Purpose: Centralizes the newsletter form so any copy change, accessibility fix, or form provider update only needs to happen once in one file.
Output: 1 new partial file; 7 updated templates each using {% include 'partials/newsletter-cta.njk' %}.
</objective>

<execution_context>
@/home/timothywinslowlinux/.claude/get-shit-done/workflows/execute-plan.md
@/home/timothywinslowlinux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-component-consistency/08-CONTEXT.md
</context>

<interfaces>
<!-- Partial scope (from CONTEXT.md locked decision): -->
<!-- newsletter-cta.njk = the signup form ONLY -->
<!-- NOT the "next steps / continue learning" article link grid -->
<!-- The next-steps grid stays inline per page -->

<!-- Representative newsletter form markup (from about.njk .nl-section): -->
```html
<form class="nl-form-wrapper" aria-label="Newsletter signup" onsubmit="return false">
  <label for="nl-email" id="nl-email-label" class="form-label">Email address</label>
  <div class="nl-form">
    <input type="email" id="nl-email" name="email" placeholder="Your email address" autocomplete="email"
      aria-required="true" aria-labelledby="nl-email-label">
    <button type="submit" class="btn btn-primary">Subscribe Free</button>
  </div>
</form>
<p class="form-fine">No spam. Never sold. Educational content only — not insurance advice. Unsubscribe anytime.</p>
```

<!-- The partial should include a heading, the form, and the fine-print -->
<!-- Wrapper class normalization: use .newsletter-cta as the canonical section/div class -->
<!-- (Claude's discretion per CONTEXT.md — normalizing .nl-section / .newsletter-cta / .article-newsletter-block) -->

<!-- Page-specific context for what to KEEP vs REPLACE: -->

| Page | Wrapper | Keep (not in partial) | Replace with partial |
|------|---------|----------------------|---------------------|
| index.njk | .section.newsletter-cta | Nothing extra | Entire section content (heading + form) |
| about.njk | .nl-section | Intro paragraph above form | Form + fine print only |
| work-with-me.njk | .nl-section | Intro paragraph above form | Form + fine print only |
| medicare-101.njk | .next-steps | next-steps-grid (article links) | .article-newsletter-block content |
| medigap-vs-mapd.njk | .next-steps | next-steps-grid (article links) | form block at bottom |
| enrollment-periods.njk | .next-steps | next-steps-grid (article links) | form block at bottom |
| part-d.njk | .next-steps | next-steps-grid (article links) | form block at bottom |

<!-- Input ID uniqueness: use a stable id like "nl-email" in the partial -->
<!-- Pages that previously used "footer-email" as the input id: update to nl-email -->
<!-- (Only one newsletter form per page, so no duplicate ID risk) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create newsletter-cta.njk partial</name>
  <files>src/_includes/partials/newsletter-cta.njk</files>
  <action>
    Create the file `src/_includes/partials/newsletter-cta.njk` with the canonical newsletter signup form. The partial should be self-contained: heading, form, and fine print. It does NOT include the outer section wrapper (callers manage their own wrapper).

    Partial content:
    ```html
    <span class="nl-section-label">Free Newsletter</span>
    <h2 id="nl-heading">Get updates delivered every week.</h2>
    <p class="nl-intro">Medicare changes every year. Stay ahead of it — enrollment deadlines, plan changes, drug cost updates, and plain-English explanations of what it all means for you.</p>
    <form class="nl-form-wrapper" aria-label="Newsletter signup" onsubmit="return false">
      <label for="nl-email" id="nl-email-label" class="form-label">Email address</label>
      <div class="nl-form">
        <input type="email" id="nl-email" name="email" placeholder="Your email address" autocomplete="email"
          aria-required="true" aria-labelledby="nl-email-label">
        <button type="submit" class="btn-primary">Subscribe Free</button>
      </div>
    </form>
    <p class="form-fine">No spam. Never sold. Educational content only — not insurance advice. Unsubscribe anytime.</p>
    ```

    Note: `btn-primary` single class (not `btn btn-primary`) — consistent with PAGE-05 cleanup in plan 08-01.
  </action>
  <verify>
    <automated>ls /home/timothywinslowlinux/retiree-report/src/_includes/partials/newsletter-cta.njk && grep -c "nl-form-wrapper\|form-fine" /home/timothywinslowlinux/retiree-report/src/_includes/partials/newsletter-cta.njk</automated>
  </verify>
  <done>File exists. Grep confirms nl-form-wrapper and form-fine are both present in the partial.</done>
</task>

<task type="auto">
  <name>Task 2: Wire partial into all 7 pages</name>
  <files>src/index.njk, src/pages/about.njk, src/pages/work-with-me.njk, src/pages/medicare-101.njk, src/pages/medigap-vs-mapd.njk, src/pages/enrollment-periods.njk, src/pages/part-d.njk</files>
  <action>
    For each page, remove the inline newsletter form block and replace with the Nunjucks include. Keep the outer section wrapper and any per-page intro text that is NOT part of the shared form.

    **index.njk:**
    Locate the `<section class="section newsletter-cta" ...>` block. Replace the inner content (heading, form, fine print) with `{% include 'partials/newsletter-cta.njk' %}`. Keep the outer `<section>` and `<div class="newsletter-inner">` wrapper intact.

    **about.njk:**
    Locate the `.nl-section` section. Keep the outer `<section class="nl-section" ...>` and `<div class="container">` wrapper. Remove the inline heading, form, and fine-print. Replace with `{% include 'partials/newsletter-cta.njk' %}`.

    **work-with-me.njk:**
    Locate the `.nl-section` section. Keep the outer `<section class="nl-section" ...>` and `<div class="container">` wrapper. Remove the inline heading, form, and fine-print. Replace with `{% include 'partials/newsletter-cta.njk' %}`.

    **medicare-101.njk:**
    Locate the `.article-newsletter-block` div inside `.next-steps`. Remove the entire `.article-newsletter-block` div's inner content (heading, form, fine print). Replace with `{% include 'partials/newsletter-cta.njk' %}` inside the `.article-newsletter-block` div (or replace the div with the include directly). Keep the `.next-steps` section and `.next-steps-grid` intact.

    **medigap-vs-mapd.njk, enrollment-periods.njk, part-d.njk:**
    Same as medicare-101.njk pattern — locate the newsletter form block at the bottom of the `.next-steps` section. Replace the form block inner content with `{% include 'partials/newsletter-cta.njk' %}`. Keep the `.next-steps` section and article links grid intact.

    After updating each file, confirm the inline `nl-form-wrapper` form markup is gone and the include statement is present.
  </action>
  <verify>
    <automated>grep -rl "include 'partials/newsletter-cta.njk'" /home/timothywinslowlinux/retiree-report/src/ | wc -l</automated>
  </verify>
  <done>Command returns 7 (all 7 pages use the partial). Build succeeds. Grep for "nl-form-wrapper" in page files returns only the partial itself, not any individual page template.</done>
</task>

</tasks>

<verification>
```
cd /home/timothywinslowlinux/retiree-report && npx @11ty/eleventy --quiet 2>&1 | tail -5
```
Build must complete with 0 errors.

Confirm all 7 pages use the partial:
```
grep -rl "include 'partials/newsletter-cta.njk'" /home/timothywinslowlinux/retiree-report/src/
```
Must return exactly 7 file paths.

Confirm no inline form markup remains in page files:
```
grep -rn "nl-form-wrapper" /home/timothywinslowlinux/retiree-report/src/pages/ /home/timothywinslowlinux/retiree-report/src/index.njk
```
Must return zero matches (form markup only lives in the partial).
</verification>

<success_criteria>
- src/_includes/partials/newsletter-cta.njk exists with heading, form, fine print
- All 7 pages use {% include 'partials/newsletter-cta.njk' %}
- No inline newsletter form markup remains in any page template
- Next-steps article link grids on guide pages are preserved unchanged
- Eleventy build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-component-consistency/08-04-SUMMARY.md`
</output>
