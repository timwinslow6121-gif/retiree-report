---
phase: 08-component-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/enrollment-periods.njk
  - src/pages/medicare-101.njk
  - src/pages/medigap-vs-mapd.njk
  - src/pages/work-with-me.njk
  - src/pages/newsletter.njk
  - src/pages/about.njk
  - assets/css/pages.css
autonomous: true
requirements:
  - PAGE-02

must_haves:
  truths:
    - "Every callout box across the site uses the .callout base class with a modifier — no .honest-box, .real-story, or .mission-block class appears in any template"
    - "New CSS modifier blocks exist for .callout-story, .callout-honest, and .callout-mission, inheriting from the .callout base"
    - "All inline style='margin-top:20px' attributes are removed from callout elements — spacing is handled by CSS"
    - "Existing callout-insight, callout-warning, and callout-myth elements continue to render correctly"
    - "Accessibility attributes (role, aria-labelledby, aria-label) are preserved on all renamed elements"
  artifacts:
    - path: "src/pages/enrollment-periods.njk"
      provides: "real-story renamed to callout callout-story"
      contains: "callout-story"
    - path: "src/pages/medicare-101.njk"
      provides: "callout-quote gets .callout base class added"
      contains: "callout callout-quote"
    - path: "src/pages/medigap-vs-mapd.njk"
      provides: "callout-quote gets .callout base class added"
      contains: "callout callout-quote"
    - path: "src/pages/work-with-me.njk"
      provides: "honest-box renamed to callout callout-honest"
      contains: "callout-honest"
    - path: "src/pages/newsletter.njk"
      provides: "honest-box renamed to callout callout-honest"
      contains: "callout-honest"
    - path: "src/pages/about.njk"
      provides: "mission-block renamed to callout callout-mission"
      contains: "callout-mission"
    - path: "assets/css/pages.css"
      provides: "New modifier blocks; old one-off blocks deleted"
  key_links:
    - from: "enrollment-periods.njk .callout.callout-story"
      to: "assets/css/pages.css .callout-story modifier"
      via: "CSS class match"
      pattern: "callout-story"
    - from: "work-with-me.njk .callout.callout-honest"
      to: "assets/css/pages.css .callout-honest modifier"
      via: "CSS class match"
      pattern: "callout-honest"
    - from: "about.njk .callout.callout-mission"
      to: "assets/css/pages.css .callout-mission modifier"
      via: "CSS class match"
      pattern: "callout-mission"
---

<objective>
Unify all one-off callout box classes under the canonical `.callout` + modifier system. Rename four one-off classes (.real-story, .callout-quote standalone, .honest-box, .mission-block) in six template files. Add corresponding CSS modifier blocks to pages.css and delete the superseded one-off CSS blocks. Remove inline style="margin-top:20px" from all callout elements as part of the same pass.

Purpose: Eliminates the four one-off callout classes so any future style change to callout boxes requires editing only the callout system in pages.css, not hunting scattered one-off blocks.
Output: 6 updated templates; pages.css with 3 new modifier blocks and 3 deleted one-off blocks; zero inline margin styles on callout elements.
</objective>

<execution_context>
@/home/timothywinslowlinux/.claude/get-shit-done/workflows/execute-plan.md
@/home/timothywinslowlinux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-component-consistency/08-CONTEXT.md
</context>

<interfaces>
<!-- Existing callout base CSS (from pages.css ~line 1108): -->
```css
.callout { border-radius: var(--radius); padding: 24px 28px; margin: 32px 0; }
.callout-label { font-size: 0.65rem; font-weight: 700; letter-spacing: 0.35em; text-transform: uppercase; margin-bottom: 10px; display: block; }
.callout p { margin: 0; font-size: 0.95rem; color: var(--text-secondary); }
.callout p + p { margin-top: 10px; }
.callout-insight { background: #FDF6E3; border-left: 4px solid var(--gold); }
.callout-insight .callout-label { color: var(--gold); }
.callout-warning { background: var(--warn-bg); border-left: 4px solid var(--warn); }
.callout-warning .callout-label { color: var(--warn); }
.callout-myth { background: #EEF2F8; border-left: 4px solid var(--navy); }
.callout-myth .callout-label { color: var(--navy); }
.callout-quote { background: var(--navy); border-radius: var(--radius); padding: 32px 28px; margin: 40px 0; }
.callout-quote blockquote { ... }
.callout-quote cite { ... }
```

<!-- One-off block inventory and their new class names: -->

| Old class | File(s) | New class | Label element |
|-----------|---------|-----------|---------------|
| .real-story | enrollment-periods.njk | .callout.callout-story | .callout-label (rename from .real-story-label) |
| .callout-quote (standalone) | medicare-101.njk, medigap-vs-mapd.njk | .callout.callout-quote | no label element, has blockquote+cite |
| .honest-box | work-with-me.njk, newsletter.njk | .callout.callout-honest | .callout-label (rename from .honest-box-label) |
| .mission-block | about.njk | .callout.callout-mission | .callout-label (rename from .mission-label) |

<!-- IMPORTANT: Preserve these accessibility attributes on rename: -->
<!-- enrollment-periods.njk .real-story: role="region" aria-label="Real client story" -->
<!-- work-with-me.njk .honest-box: role="region" aria-labelledby="notfor-heading" -->
<!-- about.njk .mission-block: role="region" aria-labelledby="mission-heading" -->

<!-- New CSS modifier blocks to add (model after existing modifiers): -->

.callout-story — dark navy bg like .real-story, gold label, muted text:
  background: var(--navy); dark bg with decorative quotation mark ::before pseudo
  .callout-story .callout-label { color: var(--gold); }
  .callout-story p { color: var(--text-on-dark-muted); }

.callout-honest — dark navy bg like .honest-box, gold label, gradient ::before:
  background: var(--navy); radial gradient ::before
  .callout-honest .callout-label { color: var(--gold); }
  .callout-honest h3 { color: var(--gold-pale); }
  .callout-honest p { color: var(--text-on-dark-muted); }

.callout-mission — dark navy bg like .mission-block, prominent heading + gold rule:
  background: var(--navy); radial gradient top-right ::before
  .callout-mission .callout-label { color: var(--gold); }
  .callout-mission h2 { color: var(--gold-pale); font-size: clamp(1.3rem, 3.5vw, 1.9rem); }
  .callout-mission p { color: var(--text-on-dark-muted); line-height: 1.8; }
  Use padding: 48px 40px (matching .mission-block visual weight)

<!-- CSS blocks to DELETE after new modifiers added: -->
- .real-story block and all .real-story-* sub-selectors
- .honest-box block and all .honest-box-* sub-selectors
- .mission-block block and all .mission-block-* sub-selectors + .mission-label
- (Do NOT delete .callout-quote CSS — it already uses the right selector name; just ensure .callout base is in markup)

<!-- Inline styles to remove from callout elements: -->
- enrollment-periods.njk lines 162, 254, 442, 636: style="margin-top:20px" on .callout elements
- Add margin-top: 20px to .callout + .callout margin-top rule in CSS if not already handled by .callout { margin: 32px 0; }
  (The existing margin: 32px 0 on .callout covers spacing — simply remove the inline attributes)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename one-off callout classes in all 6 template files</name>
  <files>src/pages/enrollment-periods.njk, src/pages/medicare-101.njk, src/pages/medigap-vs-mapd.njk, src/pages/work-with-me.njk, src/pages/newsletter.njk, src/pages/about.njk</files>
  <action>
    Perform the following markup renames in each file. Preserve all attributes not explicitly listed for change.

    **enrollment-periods.njk:**
    - `class="real-story"` → `class="callout callout-story"` (keep role="region" aria-label="Real client story")
    - `class="real-story-label"` → `class="callout-label"`
    - Remove `style="margin-top:20px"` from every `.callout` div element (4 occurrences on lines approx 162, 254, 442, 636)

    **medicare-101.njk:**
    - `class="callout-quote"` → `class="callout callout-quote"` (keep role="figure" aria-label attribute)

    **medigap-vs-mapd.njk:**
    - `class="callout-quote"` → `class="callout callout-quote"` (keep role="figure" aria-label attribute)

    **work-with-me.njk:**
    - `class="honest-box"` → `class="callout callout-honest"` (keep role="region" aria-labelledby="notfor-heading")
    - `class="honest-box-label"` → `class="callout-label"`

    **newsletter.njk:**
    - `class="honest-box"` → `class="callout callout-honest"`
    - `class="honest-box-label"` → `class="callout-label"`

    **about.njk:**
    - `class="mission-block"` → `class="callout callout-mission"` (keep role="region" aria-labelledby="mission-heading")
    - `class="mission-label"` → `class="callout-label"`

    After each file edit, grep the file to confirm the old class name no longer appears.
  </action>
  <verify>
    <automated>grep -rn "real-story\|honest-box\|mission-block\|style=\"margin-top:20px\"" /home/timothywinslowlinux/retiree-report/src/pages/ && echo "VIOLATIONS FOUND" || echo "CLEAN"</automated>
  </verify>
  <done>Zero occurrences of .real-story, .honest-box, or .mission-block in any template. Zero occurrences of style="margin-top:20px" on callout elements. All accessibility attributes (role, aria-labelledby, aria-label) preserved on renamed elements.</done>
</task>

<task type="auto">
  <name>Task 2: Add new CSS modifier blocks and delete superseded one-off blocks in pages.css</name>
  <files>assets/css/pages.css</files>
  <action>
    Add three new CSS modifier blocks to pages.css, immediately after the existing `.callout-quote` block (to keep all callout modifiers together). Then delete the superseded one-off CSS blocks.

    **Add these three modifier blocks after .callout-quote:**

    ```css
    /* ── CALLOUT: STORY ── */
    .callout-story {
      background: var(--navy);
      padding: 36px 28px;
      margin: 40px 0;
      position: relative;
      overflow: hidden;
    }
    .callout-story::before {
      content: '"';
      position: absolute;
      top: -20px;
      left: 16px;
      font-family: var(--font-display);
      font-size: 10rem;
      color: rgba(232,201,122,0.06);
      line-height: 1;
      pointer-events: none;
    }
    .callout-story .callout-label { color: var(--gold); }
    .callout-story p { color: var(--text-on-dark-muted); font-size: 0.95rem; line-height: 1.8; margin-bottom: 12px; }
    .callout-story p:last-child { margin: 0; }
    .callout-story strong { color: var(--gold-pale); }

    /* ── CALLOUT: HONEST ── */
    .callout-honest {
      background: var(--navy);
      padding: 40px;
      position: relative;
      overflow: hidden;
    }
    .callout-honest::before {
      content: '';
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at bottom right, rgba(184,148,58,0.08), transparent 60%);
      pointer-events: none;
    }
    .callout-honest .callout-label { color: var(--gold); }
    .callout-honest h3 { color: var(--gold-pale); margin-bottom: 16px; font-size: clamp(1.05rem, 2.5vw, 1.3rem); }
    .callout-honest p { color: var(--text-on-dark-muted); font-size: 0.92rem; margin-bottom: 12px; }

    /* ── CALLOUT: MISSION ── */
    .callout-mission {
      background: var(--navy);
      padding: 48px 40px;
      margin: 48px 0;
      position: relative;
      overflow: hidden;
    }
    .callout-mission::before {
      content: '';
      position: absolute;
      top: 0; right: 0;
      width: 200px; height: 200px;
      background: radial-gradient(circle at top right, rgba(184,148,58,0.12), transparent 70%);
      pointer-events: none;
    }
    .callout-mission .callout-label { color: var(--gold); }
    .callout-mission h2 { color: var(--gold-pale); font-size: clamp(1.3rem, 3.5vw, 1.9rem); margin-bottom: 20px; line-height: 1.25; }
    .callout-mission p { color: var(--text-on-dark-muted); font-size: 0.95rem; line-height: 1.8; margin-bottom: 14px; }
    ```

    **Delete these entire CSS blocks (including all sub-selectors):**
    - The `.real-story` block and all `.real-story-*` rules (approx lines 1362–1401)
    - The `.honest-box` block and all `.honest-box-*` rules (approx lines 3556–3592)
    - The `.mission-block` block and all `.mission-block-*` and `.mission-label` rules (approx lines 819–858)

    Do NOT delete the `.callout-quote` CSS block — only the markup changed (added `.callout` base class), the CSS selector is already correct.

    After editing, confirm the file no longer contains `.real-story`, `.honest-box`, or `.mission-block` selectors.
  </action>
  <verify>
    <automated>grep -n "\.real-story\|\.honest-box\|\.mission-block\|\.mission-label" /home/timothywinslowlinux/retiree-report/assets/css/pages.css && echo "STALE SELECTORS FOUND" || echo "CLEAN"</automated>
  </verify>
  <done>pages.css contains .callout-story, .callout-honest, and .callout-mission modifier blocks. The .real-story, .honest-box, .mission-block, and .mission-label selector blocks are deleted. Eleventy build succeeds with no errors.</done>
</task>

</tasks>

<verification>
```
cd /home/timothywinslowlinux/retiree-report && npx @11ty/eleventy --quiet 2>&1 | tail -5
```
Build must complete with 0 errors. Confirm _site/ was generated.

Then run:
```
grep -rn "honest-box\|real-story\|mission-block" /home/timothywinslowlinux/retiree-report/src/ /home/timothywinslowlinux/retiree-report/assets/
```
Must return zero matches.
</verification>

<success_criteria>
- Zero occurrences of .honest-box, .real-story, or .mission-block in any src/ template or assets/css/ file
- .callout-story, .callout-honest, and .callout-mission modifier blocks present in pages.css
- All inline style="margin-top:20px" removed from callout elements
- All accessibility attributes (role, aria-labelledby, aria-label) preserved
- Eleventy build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-component-consistency/08-02-SUMMARY.md`
</output>
