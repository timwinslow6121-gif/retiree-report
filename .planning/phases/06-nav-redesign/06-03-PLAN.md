---
phase: 06-nav-redesign
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - assets/css/main.css
  - assets/css/pages.css
autonomous: true
requirements:
  - NAV-06

must_haves:
  truths:
    - "The logo area (badge, text, name) is styled from exactly one authoritative CSS block in main.css"
    - "No .nav-logo-* rules remain scattered across pages.css"
    - "Logo tokens use the canonical --color-* names from main.css, not the legacy --gold-light/--navy-mid shorthand variants"
  artifacts:
    - path: "assets/css/main.css"
      provides: "Single authoritative .nav-logo, .nav-logo-badge, .nav-logo-text, .nav-logo-the, .nav-logo-name block"
      contains: "nav-logo-badge"
    - path: "assets/css/pages.css"
      provides: "No .nav-logo-* rules (all removed)"
  key_links:
    - from: "assets/css/main.css nav-logo block"
      to: "base.njk .nav-logo markup"
      via: "class selectors — .nav-logo, .nav-logo-badge, .nav-logo-text, .nav-logo-the, .nav-logo-name"
      pattern: "nav-logo"
---

<objective>
Consolidate all `.nav-logo-*` CSS rules from the 4+ duplicate blocks scattered across pages.css into a single authoritative block in main.css. Update token references from legacy shorthand (`--gold-light`, `--navy-mid`) to the canonical `--color-*` tokens. Remove all duplicate blocks from pages.css.

Purpose: The logo was the most duplicated CSS pattern in the codebase — the same 5-6 rules appeared verbatim at 4+ locations in pages.css, causing maintenance risk. A single authoritative block in main.css is the correct home.
Output: Consolidated main.css nav-logo block with canonical tokens. pages.css free of nav-logo duplication.
</objective>

<execution_context>
@/home/timothywinslowlinux/.claude/get-shit-done/workflows/execute-plan.md
@/home/timothywinslowlinux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-nav-redesign/06-CONTEXT.md
@.planning/phases/06-nav-redesign/06-01-SUMMARY.md
@.planning/phases/06-nav-redesign/06-02-SUMMARY.md

<interfaces>
<!-- Token mapping — old shorthand to canonical names -->
Legacy (pages.css)            → Canonical (main.css :root)
--gold-light                  → --color-gold-light   (#D4AE5A)
--gold-pale                   → --color-gold-light   (same value — use --color-gold-light)
--navy-mid                    → --color-navy-mid      (#1a3560)

Note: main.css :root defines:
  --color-navy-mid: #1a3560
  --color-gold-light: #D4AE5A
  (No --color-gold-pale — use --color-gold-light as the equivalent)

Current base.njk markup (logo structure — DO NOT CHANGE):
```html
<a href="/" class="nav-logo" aria-label="The Retiree Report — Home">
  <div class="nav-logo-badge" aria-hidden="true">R</div>
  <div class="nav-logo-text">
    <span class="nav-logo-the">The</span>
    <span class="nav-logo-name">Retiree Report</span>
  </div>
</a>
```

Authoritative CSS to write in main.css (after the .nav-header-cta block added by Plan 02):
```css
/* ── Nav Logo ─────────────────────────────────── */
.nav-logo {
  display: flex;
  align-items: center;
  gap: 12px;
  text-decoration: none;
  min-height: var(--tap-min);
  flex-shrink: 0;
}

.nav-logo-badge {
  width: 38px;
  height: 38px;
  background: var(--color-navy-mid);
  border: 1px solid rgba(232, 201, 122, 0.35);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-serif);
  font-weight: 900;
  font-size: 1.1rem;
  color: var(--color-gold-light);
}

.nav-logo-text {
  display: flex;
  flex-direction: column;
  line-height: 1.1;
}

.nav-logo-the {
  font-size: 0.55rem;
  font-weight: 300;
  letter-spacing: 0.5em;
  text-transform: uppercase;
  color: var(--color-gold-light);
}

.nav-logo-name {
  font-family: var(--font-serif);
  font-weight: 900;
  font-size: 1rem;
  color: var(--color-gold-light);
  white-space: nowrap;
}
```

Note: Use `var(--font-serif)` for badge and name (the display/serif font is --font-serif in main.css).
Check: `grep -n "font-serif\|font-display" /home/timothywinslowlinux/retiree-report/assets/css/main.css | head -5` to confirm token name before writing.

Duplicate blocks in pages.css to remove — search for all `.nav-logo {` occurrences:
Lines approximately: 129, 750, 1196, 1645, 1959, 2507 — and their associated .nav-logo-badge,
.nav-logo-text, .nav-logo-the, .nav-logo-name rules in the same block.
Search pattern: `grep -n "nav-logo" /home/timothywinslowlinux/retiree-report/assets/css/pages.css`
to find exact line numbers before removing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write authoritative nav-logo block in main.css</name>
  <files>assets/css/main.css</files>
  <action>
First, confirm the exact font token name:
```bash
grep -n "font-serif\|font-display\|--font-" /home/timothywinslowlinux/retiree-report/assets/css/main.css | head -10
```

In `assets/css/main.css`, locate the nav section (near the .nav-toggle rules, after the .nav-header-cta block added by Plan 02). Check whether a `.nav-logo` block already exists in main.css from a previous iteration. If it does NOT exist, add the authoritative block after the .nav-header-cta rules.

Write the canonical block using the CSS from the interfaces section above:
- Use the correct font token (whichever of --font-serif / --font-display controls Playfair Display)
- Replace --gold-light → --color-gold-light
- Replace --gold-pale → --color-gold-light
- Replace --navy-mid → --color-navy-mid
- Add flex-shrink: 0 to .nav-logo (prevents logo from squishing on narrow viewports)
- Badge uses border-radius: var(--radius-sm) (consistent with main.css token convention)

This block does NOT need a media query — the logo renders the same at all sizes.

If main.css already has some nav-logo rules from pages.css era, REPLACE them entirely with this canonical block.
  </action>
  <verify>
    <automated>grep -n "nav-logo\|nav-logo-badge\|nav-logo-name" /home/timothywinslowlinux/retiree-report/assets/css/main.css | head -20</automated>
  </verify>
  <done>main.css contains a single .nav-logo block with .nav-logo-badge, .nav-logo-text, .nav-logo-the, .nav-logo-name rules using --color-gold-light and --color-navy-mid tokens</done>
</task>

<task type="auto">
  <name>Task 2: Remove all duplicate nav-logo blocks from pages.css</name>
  <files>assets/css/pages.css</files>
  <action>
Find all nav-logo block locations in pages.css:
```bash
grep -n "nav-logo" /home/timothywinslowlinux/retiree-report/assets/css/pages.css
```

This will return line numbers for all .nav-logo, .nav-logo-badge, .nav-logo-text, .nav-logo-the, .nav-logo-name occurrences. There are approximately 4-5 duplicate clusters.

For each cluster, remove the entire group of rules (.nav-logo through .nav-logo-name) from pages.css. Each cluster is typically 5-8 lines (sometimes single-line declarations, sometimes multi-line).

IMPORTANT: Only remove .nav-logo-* rules. Do NOT remove adjacent .nav-toggle span or .site-nav__list rules in pages.css if they are different rules in the same section — check line-by-line before deleting.

After removal, run:
```bash
grep -c "nav-logo" /home/timothywinslowlinux/retiree-report/assets/css/pages.css
```

The result should be 0. If non-zero, there are remaining instances — find and remove them.

Also check for and remove any duplicate .skip-link, .nav-toggle span, .site-nav__list a, .nav-menu-cta, and .btn / .btn-primary blocks that were co-located with the nav-logo duplicates in pages.css — these are also duplicates of main.css rules. Only remove rules that are exact duplicates of main.css definitions.

Strategy for large blocks: If multiple rules cluster together in pages.css as a copy of a main.css section, remove the entire cluster rather than picking individual lines — it is safer and produces cleaner CSS.
  </action>
  <verify>
    <automated>grep -c "nav-logo" /home/timothywinslowlinux/retiree-report/assets/css/pages.css</automated>
  </verify>
  <done>grep -c "nav-logo" pages.css returns 0 — all duplicate nav-logo rules removed from pages.css</done>
</task>

<task type="auto">
  <name>Task 3: Build and verify logo renders correctly</name>
  <files>assets/css/main.css, assets/css/pages.css</files>
  <action>
Run the Eleventy build:
```bash
cd /home/timothywinslowlinux/retiree-report && npm run build 2>&1 | tail -20
```

Verify that the built CSS files do not contain nav-logo duplication and the build passes cleanly.

Check pages.css for any remaining nav-logo references:
```bash
grep -c "nav-logo" /home/timothywinslowlinux/retiree-report/assets/css/pages.css
```
Expected: 0

Check main.css for the canonical nav-logo block:
```bash
grep -c "nav-logo" /home/timothywinslowlinux/retiree-report/assets/css/main.css
```
Expected: 5-6 (one per selector: .nav-logo, .nav-logo-badge, .nav-logo-text, .nav-logo-the, .nav-logo-name, plus .nav-logo property lines)

Visually note: The logo badge "R", "The" supertext, and "Retiree Report" wordmark are controlled by these CSS rules. The consolidated rules preserve the exact same visual appearance as the pages.css duplicates — only the token names and file location changed.
  </action>
  <verify>
    <automated>cd /home/timothywinslowlinux/retiree-report && npm run build 2>&1 | grep -E "Wrote|Finished|error|Error" | tail -5</automated>
  </verify>
  <done>Build passes with no errors. pages.css has 0 nav-logo references. main.css has authoritative nav-logo block.</done>
</task>

</tasks>

<verification>
1. `grep -c "nav-logo" assets/css/pages.css` returns 0
2. `grep -n "nav-logo-badge\|nav-logo-name\|color-navy-mid\|color-gold-light" assets/css/main.css` — confirms canonical block with correct tokens
3. `npm run build` exits 0
4. No visual regression — logo markup in base.njk unchanged, only CSS source moved
</verification>

<success_criteria>
- Single authoritative .nav-logo-* block in main.css using --color-gold-light and --color-navy-mid tokens (NAV-06)
- Zero nav-logo rules remaining in pages.css
- Build passes cleanly
- Logo visual appearance preserved exactly (badge 38x38px, Playfair Display, gold text)
</success_criteria>

<output>
After completion, create `.planning/phases/06-nav-redesign/06-03-SUMMARY.md` following the summary template.
</output>
